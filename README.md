# isoCamp
isoCamp es un framework de compresión/normalización combinatoria — usa Z₇ + O_n para generar parámetros estructurados (no aleatorios) que luego se mapean a rotaciones SU(2)/cuaterniónicas. 
